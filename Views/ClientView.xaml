<UserControl x:Class="SB_Module_10.Views.ClientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SB_Module_10.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500"
             Background="Transparent">

    <Grid>
        <StackPanel Orientation="Vertical">
            <TextBlock Text="Карта клиента"
                       HorizontalAlignment="Center"
                       FontSize="20"
                       FontWeight="Bold"/>

            <Border BorderThickness="0,2,0,0"
                    BorderBrush="Violet">
                <Grid>

                    <Grid Style="{StaticResource InvisibleGrid}" Tag="{Binding GridVisibility}">
                        
                    <StackPanel Orientation="Vertical">
                        <GroupBox BorderThickness="2" BorderBrush="#FF137FDC">
                            
                                <GroupBox.Header>
                                    <TextBlock Text="Основная информация"
                                               Foreground="#FF137FDC"
                                               FontFamily="Arial"
                                               FontSize="16"
                                               FontWeight="Black"/>
                                </GroupBox.Header>
                            
                            <StackPanel Orientation="Vertical">
                                
                                <DockPanel Margin="10,10,10,0" LastChildFill="True" Height="20">
                                    <TextBlock Text="Фамилия:" 
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <Border Style="{StaticResource UserFieldsBorder}">                               
                                        <TextBox Text="{Binding SelectedClient.Surname}"
                                                 Style="{StaticResource UserTextBoxInfo}"/>
                                    </Border>                                  
                                </DockPanel>
                                <DockPanel Margin="10,10,10,0" LastChildFill="True" Height="20">
                                    <TextBlock Text="Имя:" 
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <Border Style="{StaticResource UserFieldsBorder}">                               
                                        <TextBox Text="{Binding SelectedClient.Name}"
                                                 Style="{StaticResource UserTextBoxInfo}"/>
                                    </Border>                                  
                                </DockPanel>
                                <DockPanel Margin="10,10,10,0" LastChildFill="True" Height="20">
                                    <TextBlock Text="Отчество:" 
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <Border Style="{StaticResource UserFieldsBorder}">                               
                                        <TextBox Text="{Binding SelectedClient.Patronymics}"
                                                 Style="{StaticResource UserTextBoxInfo}"/>
                                    </Border>                                  
                                </DockPanel>
                                <DockPanel Margin="10,10,10,0" LastChildFill="True" Height="20">
                                    <TextBlock Text="Телефон:" 
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <Border Style="{StaticResource UserFieldsBorder}">                               
                                        <TextBox Text="{Binding SelectedClient.PhoneNumber}"
                                                 Style="{StaticResource UserTextBoxInfo}"/>
                                    </Border>                                  
                                </DockPanel>
                                <DockPanel Margin="10,10,10,0" LastChildFill="True" Height="20">
                                    <TextBlock Text="Серия паспорта:" 
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <Border Style="{StaticResource UserFieldsBorder}">                               
                                        <TextBox Text="{Binding SelectedClient.PassportSeries}"
                                                 Style="{StaticResource UserTextBoxInfo}"/>
                                    </Border>                                  
                                </DockPanel>
                                <DockPanel Margin="10,10,10,0" LastChildFill="True" Height="20">
                                    <TextBlock Text="Номер Паспорта:" 
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <Border Style="{StaticResource UserFieldsBorder}">                               
                                        <TextBox Text="{Binding SelectedClient.PassportNumber}"
                                                 Style="{StaticResource UserTextBoxInfo}"/>
                                    </Border>                                  
                                </DockPanel>
                                
                            </StackPanel>
                            
                        </GroupBox>
                        
                        <GroupBox BorderThickness="2" BorderBrush="#FF137FDC">
                            
                            <GroupBox.Header>
                                <TextBlock Text="История изменений"
                                           Foreground="#FF137FDC"
                                           FontFamily="Arial"
                                           FontSize="16"
                                           FontWeight="Black"/>
                            </GroupBox.Header>
                            
                            <StackPanel Orientation="Vertical">
                                
                                <DockPanel Margin="10,10,10,0" LastChildFill="True">
                                    <TextBlock Text="Дата последнего изменения:"                                          
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <TextBox Text="{Binding SelectedClient.DataChangedTime,
                                        Converter={StaticResource DateTimeCNV}}"
                                             Style="{StaticResource UserTextBoxInfo}"/>
                                </DockPanel>
                                
                                <DockPanel Margin="10,10,10,0" LastChildFill="True">
                                    <TextBlock Text="Инициатор изменений:"                                          
                                               Style="{StaticResource UserTextBlockInfo}"/>
                                    <TextBox Text="{Binding SelectedClient.ChangeInitiator}"
                                             Style="{StaticResource UserTextBoxInfo}"/>
                                </DockPanel>

                                 <ListView ItemsSource="{Binding SelectedClient.Changes}">
                                        <ListView.View>
                                            <GridView>
                                                <GridView.Columns>
                                                    <GridViewColumn Header="Тип изменений">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding Path=Key}"
                                                                           Foreground="#FF137FDC"
                                                                            FontFamily="Arial"
                                                                            FontSize="14"
                                                                            FontWeight="SemiBold"/>
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>
                                                    <GridViewColumn Header="Предыдущее значение">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding Path=Value}"/>
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>
                                                </GridView.Columns>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>


                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </Grid>
                    <Grid Style="{StaticResource VisibleGrid}" Tag="{Binding GridVisibility}">
                        <StackPanel Orientation="Vertical">

                            <GroupBox Header="Основные">
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Фамилия:"/>
                                        <TextBox Text="{Binding SelectedClient.Surname}"
                                                 IsReadOnly="True"
                                                 MinWidth="120"/>
                                        <TextBlock Text="Имя:"/>
                                        <TextBox Text="{Binding SelectedClient.Name}"
                                                 IsReadOnly="True"
                                                 MinWidth="120"/>
                                    </StackPanel>
                                    
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Отчество:"/>
                                        <TextBox Text="{Binding SelectedClient.Patronymics}"
                                                 IsReadOnly="True"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Номер телефона:"/>
                                        <TextBox Text="{Binding SelectedClient.PhoneNumber}"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Серия паспорта:"/>
                                        <TextBox Text="{Binding PassportSeries}"
                                                 IsReadOnly="True"/>
                                        <TextBlock Text="Номер паспорта:"/>
                                        <TextBox Text="{Binding PassportNumber}"
                                                 IsReadOnly="True"/>
                                    </StackPanel>
                                </StackPanel>
                            </GroupBox>
                            <GroupBox Header="История изменений">
                                <StackPanel Orientation="Vertical">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Дата последнего изменения:"/>
                                        <TextBox Text="{Binding SelectedClient.DataChangedTime}"
                                                 IsReadOnly="True"
                                                 MinWidth="120"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Инициатор изменений:"/>
                                        <TextBox Text="{Binding SelectedClient.ChangeInitiator}"
                                                 IsReadOnly="True"
                                                 MinWidth="120"/>
                                    </StackPanel>
                                    <ListView ItemsSource="{Binding SelectedClient.Changes}">
                                        <ListView.View>
                                            <GridView>
                                                <GridView.Columns>
                                                    <GridViewColumn Header="Тип изменений">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding Path=Key}"/>
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>
                                                    <GridViewColumn Header="Предыдущее значение">
                                                        <GridViewColumn.CellTemplate>
                                                            <DataTemplate>
                                                                <TextBlock Text="{Binding Path=Value}"/>
                                                            </DataTemplate>
                                                        </GridViewColumn.CellTemplate>
                                                    </GridViewColumn>
                                                </GridView.Columns>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>


                                </StackPanel>
                            </GroupBox>
                        </StackPanel>
                    </Grid>
                </Grid>
                
                
            </Border>

            <Button Content="Сохранить изменения" 
                    FontSize="24" 
                    Command="{Binding EditClientCommand}"
                    VerticalAlignment="Bottom"/>
        </StackPanel>
    </Grid>
</UserControl>
